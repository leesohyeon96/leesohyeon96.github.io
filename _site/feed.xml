<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" hreflang="ko" /><updated>2026-02-01T14:00:15+09:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">SHL Dev</title><subtitle>SHL PR | Project | Portfolio | History etc</subtitle><author><name>Admin</name></author><entry xml:lang="en"><title type="html">SRT/KTX Booking System Implementation &amp;amp; JMeter Load Test</title><link href="http://localhost:4000/en/blog/train-reservations-jmeter-test/" rel="alternate" type="text/html" title="SRT/KTX Booking System Implementation &amp;amp; JMeter Load Test" /><published>2026-02-01T00:00:00+09:00</published><updated>2026-02-01T00:00:00+09:00</updated><id>http://localhost:4000/en/blog/train-reservations-jmeter-test-en</id><content type="html" xml:base="http://localhost:4000/en/blog/train-reservations-jmeter-test/"><![CDATA[<h2 id="-project-overview">🎯 Project Overview</h2>

<p>I implemented the design described in <a href="/en/blog/how-to-reserve-srt-or-ktx/">SRT/KTX Booking System Design</a> and conducted load testing using JMeter.</p>

<hr />

<h2 id="-github-repository">📦 GitHub Repository</h2>

<p><a href="https://github.com/leesohyeon96/train-reservations-jmeter-test" target="_blank">🔗 train-reservations-jmeter-test</a></p>

<hr />

<h2 id="-step-by-step-improvements">🚀 Step-by-Step Improvements</h2>

<h3 id="step1-basic-implementation">Step1: Basic Implementation</h3>
<ul>
  <li><strong>Tech Stack</strong>: JPA + Pessimistic Lock</li>
  <li><strong>Purpose</strong>: Basic concurrency control testing</li>
  <li><strong>Issues</strong>: Performance degradation and deadlocks under high traffic</li>
</ul>

<h3 id="step2-redis-queue-introduction">Step2: Redis Queue Introduction</h3>
<ul>
  <li><strong>Tech Stack</strong>: JPA + Redis Queue + Lua Script</li>
  <li><strong>Improvements</strong>:
    <ul>
      <li>Resolved concurrency conflicts</li>
      <li>Guaranteed reservation order</li>
    </ul>
  </li>
  <li><strong>Key</strong>: Atomic processing with Lua scripts</li>
</ul>

<h3 id="step3-performance-optimization">Step3: Performance Optimization</h3>
<ul>
  <li><strong>Tech Stack</strong>: JPA + Redis Cluster + Enhanced Batch Processing</li>
  <li><strong>Improvements</strong>:
    <ul>
      <li>Redis Connection Pool optimization (max 20 connections)</li>
      <li>Batch size: 100 → <strong>500</strong></li>
      <li>Processing interval: 500ms → <strong>200ms</strong></li>
      <li>Worker threads: 4 → <strong>8</strong></li>
      <li>Seat information caching (TTL: 30 minutes)</li>
      <li>Reservation history caching (TTL: 5 minutes)</li>
    </ul>
  </li>
</ul>

<h3 id="step4-1-rabbitmq-introduction">Step4-1: RabbitMQ Introduction</h3>
<ul>
  <li><strong>Tech Stack</strong>: JPA + Redis (Inventory) + RabbitMQ</li>
  <li><strong>Features</strong>:
    <ul>
      <li>Direct Exchange</li>
      <li>Dead Letter Queue (DLQ)</li>
      <li>Message persistence</li>
      <li>Management UI</li>
    </ul>
  </li>
</ul>

<h3 id="step4-2-kafka-introduction">Step4-2: Kafka Introduction</h3>
<ul>
  <li><strong>Tech Stack</strong>: JPA + Redis (Inventory) + Kafka</li>
  <li><strong>Features</strong>:
    <ul>
      <li>Parallel processing with 3 partitions</li>
      <li>Consumer Group-based scaling</li>
      <li>Order guarantee within partitions</li>
      <li>High throughput</li>
    </ul>
  </li>
</ul>

<hr />

<h2 id="-step4-1-vs-step4-2-comparison">📊 Step4-1 vs Step4-2 Comparison</h2>

<table>
  <thead>
    <tr>
      <th>Item</th>
      <th>RabbitMQ</th>
      <th>Kafka</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Structure</td>
      <td>Exchange → Queue</td>
      <td>Topic → Partition</td>
    </tr>
    <tr>
      <td>Order Guarantee</td>
      <td>Within queue</td>
      <td>Within partition</td>
    </tr>
    <tr>
      <td>Throughput</td>
      <td>Medium</td>
      <td>Very High</td>
    </tr>
    <tr>
      <td>Scalability</td>
      <td>Increase consumers</td>
      <td>Increase partitions</td>
    </tr>
    <tr>
      <td>Features</td>
      <td>Dead Letter Queue</td>
      <td>Offset management</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="-testing-method">🧪 Testing Method</h2>

<ol>
  <li>Run infrastructure with Docker Compose (Redis/RabbitMQ/Kafka)</li>
  <li>Run application</li>
  <li>Execute JMeter test plan</li>
  <li>Check performance metrics</li>
</ol>

<hr />

<h2 id="-related-posts">📝 Related Posts</h2>

<ul>
  <li><a href="/en/blog/how-to-reserve-srt-or-ktx/">SRT/KTX Booking System Design</a> - System design and architecture explanation</li>
</ul>]]></content><author><name>Sohyeon Lee</name></author><category term="free" /><summary type="html"><![CDATA[🎯 Project Overview]]></summary></entry><entry xml:lang="ko"><title type="html">성능 최적화 가이드: TPS와 응답 시간 개선하기</title><link href="http://localhost:4000/ko/blog/performance-optimization-guide/" rel="alternate" type="text/html" title="성능 최적화 가이드: TPS와 응답 시간 개선하기" /><published>2026-02-01T00:00:00+09:00</published><updated>2026-02-01T00:00:00+09:00</updated><id>http://localhost:4000/ko/blog/how-to-optimize-server</id><content type="html" xml:base="http://localhost:4000/ko/blog/performance-optimization-guide/"><![CDATA[<p><br /></p>

<h1 id="-성능-측정-지표">📊 성능 측정 지표</h1>

<p>시스템 성능은 일반적으로 두 가지 지표로 평가됨!</p>

<h2 id="응답-시간-response-time">응답 시간 (Response Time)</h2>
<p>클라이언트가 요청을 보낸 시점부터 처리 결과를 받을 때까지 걸리는 시간</p>

<h2 id="처리량-throughput">처리량 (Throughput)</h2>
<p>TPS(Transactions Per Second): 초당 처리할 수 있는 트랜잭션 개수</p>
<ul>
  <li>수치가 높을수록 더 많은 요청을 처리할 수 있음
<br /> ex) TPS 30 : 1초당 30개의 트랜잭션 처리 가능!</li>
</ul>

<p><br /></p>

<hr />

<h1 id="-tps를-높이는-일반적인-방법과-한계">🚀 TPS를 높이는 일반적인 방법과 한계</h1>

<h2 id="흔히-시도하는-방법">흔히 시도하는 방법</h2>
<ol>
  <li>서버 대수 늘리기 (스케일 아웃)</li>
  <li>쓰레드 풀 + DB 커넥션 풀 크기 늘리기</li>
</ol>

<h2 id="이-방법들의-한계">이 방법들의 한계</h2>
<p>→ DB CPU 점유율이 70~80%를 넘어가면</p>
<ul>
  <li>DB 쿼리 처리 시간이 증가</li>
  <li>전체 처리 시간이 길어짐</li>
  <li><strong>오히려 TPS가 떨어지는 역효과 발생</strong></li>
</ul>

<p><strong>⇒ 근본적으로 ‘처리 시간’ 자체를 줄여야 함</strong></p>

<p><br /></p>

<hr />

<h1 id="-처리-시간을-줄이는-방법">⚡ 처리 시간을 줄이는 방법</h1>

<h2 id="1-db-최적화">1. DB 최적화</h2>

<h3 id="1-1-쿼리-튜닝">1-1. 쿼리 튜닝</h3>

<h4 id="인덱스-최적화">인덱스 최적화</h4>
<ul>
  <li>자주 조회하는 컬럼에 적절한 인덱스 적용
    <ul>
      <li>B-tree: 범위 검색, 정렬이 필요한 경우</li>
      <li>Hash: 동등 비교(=)만 하는 경우</li>
      <li>Composite Index: 여러 컬럼을 함께 조회하는 경우</li>
    </ul>
  </li>
</ul>

<h4 id="where-join-order-by-group-by-절-최적화">WHERE, JOIN, ORDER BY, GROUP BY 절 최적화</h4>
<ul>
  <li>이들 절에 사용되는 컬럼에 인덱스 고려</li>
  <li><strong>주의</strong>: DB 옵티마이저는 조회 비율이 높으면 인덱스를 무시하고 Full Scan을 선택할 수 있음
    <ul>
      <li>DB마다 임계값이 다름 (일반적으로 전체 데이터의 5~15%)</li>
    </ul>

    <p>=&gt; [결론] 인덱스를 걸어서 조회할 때 전체 row 수의 1~5%만 조회할 수록 인덱스가 효과적인 것!</p>
  </li>
</ul>

<h4 id="불필요한-select--자제">불필요한 SELECT * 자제</h4>
<ul>
  <li>필요한 컬럼만 명시적으로 선택</li>
  <li>네트워크 전송량 감소 및 DB 메모리 효율성 향상</li>
</ul>

<h4 id="join-최적화">JOIN 최적화</h4>

<p><strong>1. JOIN 순서 최적화</strong></p>
<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- ❌ 잘못된 예: 큰 테이블을 먼저 조회</span>
<span class="k">SELECT</span> <span class="o">*</span> 
  <span class="k">FROM</span> <span class="n">orders</span> <span class="n">o</span>  <span class="c1">-- 1,000만 건</span>
  <span class="k">JOIN</span> <span class="n">countries</span> <span class="k">c</span> <span class="k">ON</span> <span class="n">o</span><span class="p">.</span><span class="n">country_id</span> <span class="o">=</span> <span class="k">c</span><span class="p">.</span><span class="n">id</span>  <span class="c1">-- 200건</span>

<span class="c1">-- ✅ 올바른 예: 작은 테이블을 먼저 조회</span>
<span class="k">SELECT</span> <span class="o">*</span> 
  <span class="k">FROM</span> <span class="n">countries</span> <span class="k">c</span>  <span class="c1">-- 200건</span>
  <span class="k">JOIN</span> <span class="n">orders</span> <span class="n">o</span> <span class="k">ON</span> <span class="k">c</span><span class="p">.</span><span class="n">id</span> <span class="o">=</span> <span class="n">o</span><span class="p">.</span><span class="n">country_id</span>  <span class="c1">-- 1,000만 건</span>
</code></pre></div></div>

<p><strong>주의사항</strong>:</p>
<ul>
  <li>PostgreSQL 등 현대 DB는 자동으로 JOIN 순서를 최적화하지만 완벽하지 않음</li>
  <li>통계 정보가 오래되었거나, 함수/서브쿼리/CTE 사용 시 최적화가 제대로 작동하지 않을 수 있음</li>
</ul>

<p><strong>2. 존재 여부 확인</strong></p>
<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- ✅ EXISTS 사용: 첫 번째 매칭만 찾으면 즉시 종료</span>
<span class="k">SELECT</span> <span class="o">*</span> 
  <span class="k">FROM</span> <span class="n">users</span> 
 <span class="k">WHERE</span> <span class="k">EXISTS</span> <span class="p">(</span>
  <span class="k">SELECT</span> <span class="mi">1</span> 
    <span class="k">FROM</span> <span class="n">orders</span> 
   <span class="k">WHERE</span> <span class="n">orders</span><span class="p">.</span><span class="n">user_id</span> <span class="o">=</span> <span class="n">users</span><span class="p">.</span><span class="n">id</span>
<span class="p">);</span>

<span class="c1">-- ❌ IN 사용: 전체 서브쿼리 결과를 메모리에 로드</span>
<span class="k">SELECT</span> <span class="o">*</span> 
  <span class="k">FROM</span> <span class="n">users</span> 
 <span class="k">WHERE</span> <span class="n">id</span> <span class="k">IN</span> <span class="p">(</span>
  <span class="k">SELECT</span> <span class="n">user_id</span> 
    <span class="k">FROM</span> <span class="n">orders</span>
<span class="p">);</span>
</code></pre></div></div>

<p><strong>⇒ 결론: JOIN은 최소화하고, 존재 확인은 EXISTS 사용, IN은 가능하면 피하기</strong></p>

<h4 id="explainprofile로-실행-계획-분석">EXPLAIN/PROFILE로 실행 계획 분석</h4>
<ul>
  <li>쿼리가 어떻게 실행되는지 확인</li>
  <li>Full Table Scan 여부 체크</li>
  <li>인덱스 사용 여부 확인</li>
</ul>

<h4 id="batch-처리">Batch 처리</h4>
<ol>
  <li>INSERT, UPDATE, DELETE 다수 건은 <strong>반드시</strong> Bulk 처리
```sql
– ❌ 개별 처리
INSERT INTO users VALUES (1, ‘A’);
INSERT INTO users VALUES (2, ‘B’);</li>
</ol>

<p>– ✅ Bulk 처리
INSERT INTO users VALUES (1, ‘A’), (2, ‘B’), (3, ‘C’);</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
2. 트랜잭션 묶기로 I/O 횟수 감소

**⇒ DB 최적화의 핵심: I/O 최소화 + CPU 부담 줄이기**

&lt;br&gt;

### 📌 I/O Bound vs CPU Bound 작업

#### I/O Bound 작업이란?
1. CPU는 거의 사용하지 않음
2. 외부 자원을 기다리는 시간이 대부분
3. 예시:
   - 네트워크 통신
   - 디스크 읽기/쓰기
   - DB 쿼리
   - 파일 I/O
   - 외부 API 응답 대기

**문제점**: I/O 대기 중 쓰레드는 아무 일도 못하고 블록됨 → 쓰레드 자원 낭비

&lt;br&gt;

### 1-2. 캐싱

메모리에 데이터를 저장해 DB 쿼리 실행 횟수를 줄임
- Redis, Memcached 등 인메모리 캐시 활용
- Application Level 캐시 (Caffeine, Guava Cache 등)

### 1-3. DB 인프라 개선

**읽기/쓰기 분리 (Read/Write Splitting)**
- Master: 쓰기 작업
- Slave(Replica): 읽기 작업
- 트래픽 분산으로 부하 감소

**스케일 업**
- CPU, 메모리, SSD 등 하드웨어 사양 향상
- 비용 대비 효과 고려 필요

### 1-4. Connection Pool 최적화

DB, HTTP Client, Redis 등의 커넥션 풀 설정 최적화
- `maximumPoolSize`: 최대 연결 수
- `minimumIdle`: 최소 유휴 연결 수
- `connectionTimeout`: 연결 대기 시간

**효과**:
- 과도한 커넥션 생성/폐기 방지
- CPU 및 메모리 절약
- 안정적인 성능 유지

&lt;br&gt;

## 2. 외부 API 호출 최적화

### 2-1. 캐싱

자주 변경되지 않는 외부 API 응답은 캐시에 저장

### 2-2. 동기 호출 제거

직접 호출 대신 메시지 큐를 통한 비동기 처리
- Kafka, RabbitMQ, AWS SQS 등 활용
- 외부 시스템 장애가 전파되지 않음

### 2-3. 비동기 처리

**Java 예시**:
```java
CompletableFuture.supplyAsync(() -&gt; {
    return externalApiService.call();
});
</code></pre></div></div>

<p><strong>중요</strong>:</p>
<ul>
  <li><strong>비동기 처리는 성능 향상이 아닌, 요청 쓰레드 보호와 격리가 목적</strong></li>
  <li>전체 처리 시간은 오히려 증가할 수 있음</li>
  <li>사용자 경험 개선 및 시스템 안정성 향상이 주 목적</li>
</ul>

<p><br /></p>

<h2 id="3-데이터-집계-및-계산-최적화">3. 데이터 집계 및 계산 최적화</h2>

<h3 id="실시간-계산의-문제점">실시간 계산의 문제점</h3>

<p>좋아요 수, 조회 수, 팔로워 수 등을 매번 계산하면:</p>
<ul>
  <li>복잡한 JOIN 또는 COUNT 쿼리 실행</li>
  <li>DB 부하 증가</li>
  <li><strong>TPS 급격히 감소</strong></li>
</ul>

<h3 id="해결-방법">해결 방법</h3>

<h4 id="pre-aggregation-사전-집계">Pre-Aggregation (사전 집계)</h4>
<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- ❌ 매번 계산</span>
<span class="k">SELECT</span> <span class="k">COUNT</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> 
  <span class="k">FROM</span> <span class="n">likes</span> 
 <span class="k">WHERE</span> <span class="n">post_id</span> <span class="o">=</span> <span class="mi">123</span><span class="p">;</span>

<span class="c1">-- ✅ 미리 계산해서 저장</span>
<span class="k">UPDATE</span> <span class="n">posts</span> 
   <span class="k">SET</span> <span class="n">like_count</span> <span class="o">=</span> <span class="n">like_count</span> <span class="o">+</span> <span class="mi">1</span> 
 <span class="k">WHERE</span> <span class="n">id</span> <span class="o">=</span> <span class="mi">123</span><span class="p">;</span>
</code></pre></div></div>

<h4 id="batch-작업으로-주기적-집계">Batch 작업으로 주기적 집계</h4>
<ul>
  <li>실시간성이 중요하지 않은 데이터는 배치로 처리</li>
  <li>예: 1시간마다 통계 재계산</li>
</ul>

<h4 id="eventually-consistent-전략">Eventually Consistent 전략</h4>
<ul>
  <li>약간의 데이터 불일치를 허용</li>
  <li>궁극적으로는 일관성 보장</li>
  <li>예: 좋아요 수가 1~2초 지연되어도 문제없는 경우</li>
</ul>

<p><br /></p>

<hr />

<h1 id="️-대기-시간을-줄이는-방법">⏱️ 대기 시간을 줄이는 방법</h1>

<p><strong>응답 시간 = 대기 시간 + 처리 시간</strong></p>

<p>대기 시간은 주로 네트워크 전송과 관련됨</p>

<h2 id="대역폭-bandwidth">대역폭 (Bandwidth)</h2>
<p>최대로 전송할 수 있는 데이터 양</p>
<ul>
  <li>대역폭이 작으면 동시 접속자 증가 시 전송 속도 급격히 저하</li>
</ul>

<h3 id="응답-크기-줄이기">응답 크기 줄이기</h3>

<p><strong>압축 (Compression)</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Content-Encoding: gzip
</code></pre></div></div>
<ul>
  <li>HTTP 응답 본문 압축</li>
  <li>일반적으로 70~90% 크기 감소</li>
</ul>

<p><strong>이미지 최적화</strong></p>
<ul>
  <li>WebP 포맷 사용</li>
  <li>적절한 해상도 및 품질 설정</li>
  <li>Lazy Loading 적용</li>
</ul>

<p><strong>JSON 응답 최적화</strong></p>
<ul>
  <li>불필요한 필드 제거</li>
  <li>데이터 구조 간소화</li>
</ul>

<h3 id="트래픽-분리">트래픽 분리</h3>

<p><strong>CDN (Content Delivery Network) 활용</strong></p>
<ul>
  <li>정적 파일(이미지, CSS, JS)은 CDN에서 제공</li>
  <li>원본 서버 부하 감소</li>
  <li>사용자와 가까운 엣지 서버에서 제공 → 지연 시간 감소</li>
</ul>

<p><strong>예시</strong>:</p>
<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- ❌ 원본 서버에서 직접 제공 --&gt;</span>
<span class="nt">&lt;img</span> <span class="na">src=</span><span class="s">"/static/images/logo.png"</span><span class="nt">&gt;</span>

<span class="c">&lt;!-- ✅ CDN을 통해 제공 --&gt;</span>
<span class="nt">&lt;img</span> <span class="na">src=</span><span class="s">"https://cdn.example.com/images/logo.png"</span><span class="nt">&gt;</span>
</code></pre></div></div>

<h3 id="대역폭-늘리기">대역폭 늘리기</h3>
<p>인스턴스 스펙 업그레이드</p>
<ul>
  <li>AWS EC2의 경우 인스턴스 타입에 따라 네트워크 대역폭 다름</li>
  <li>예: t3.medium (최대 5 Gbps) → c5n.large (최대 25 Gbps)</li>
  <li><strong>단점</strong>: 비용 증가</li>
</ul>

<p><br /></p>

<hr />

<h1 id="-결론">✅ 결론</h1>

<p>성능 최적화는 단순히 서버를 늘리는 것이 아니라, 다층적 접근이 필요함</p>

<h2 id="핵심-전략">핵심 전략</h2>
<ol>
  <li><strong>DB 최적화</strong>: 쿼리 튜닝, 인덱스 최적화, I/O 최소화</li>
  <li><strong>캐싱</strong>: 불필요한 연산 및 DB 조회 제거</li>
  <li><strong>비동기 처리</strong>: 쓰레드 효율성 확보 및 시스템 격리</li>
  <li><strong>사전 집계</strong>: 실시간 계산 대신 미리 계산된 결과 활용</li>
  <li><strong>네트워크 최적화</strong>: 압축, CDN, 대역폭 관리</li>
</ol>

<h2 id="주의사항">주의사항</h2>
<ul>
  <li>무조건적인 스케일 아웃보다 <strong>근본 원인 해결이 우선</strong></li>
  <li>비동기는 만능이 아님 - <strong>목적에 맞게 사용</strong></li>
  <li>캐시는 <strong>정합성 문제</strong>를 고려해야 함</li>
  <li>최적화는 <strong>측정 기반</strong>으로 진행 (추측 금지)</li>
</ul>]]></content><author><name>이소현</name></author><category term="개발" /><summary type="html"><![CDATA[]]></summary></entry><entry xml:lang="en"><title type="html">Performance Optimization Guide: Improving TPS and Response Time</title><link href="http://localhost:4000/en/blog/performance-optimization-guide/" rel="alternate" type="text/html" title="Performance Optimization Guide: Improving TPS and Response Time" /><published>2026-02-01T00:00:00+09:00</published><updated>2026-02-01T00:00:00+09:00</updated><id>http://localhost:4000/en/blog/how-to-optimize-server-en</id><content type="html" xml:base="http://localhost:4000/en/blog/performance-optimization-guide/"><![CDATA[<p><br /></p>

<h1 id="-performance-metrics">📊 Performance Metrics</h1>

<p>System performance is generally evaluated using two key metrics.</p>

<h2 id="response-time">Response Time</h2>

<p>The time from when a client sends a request until receiving the processing result</p>

<h2 id="throughput">Throughput</h2>

<p>TPS (Transactions Per Second): Number of transactions that can be processed per second</p>
<ul>
  <li>Higher values indicate better capacity to handle more requests</li>
</ul>

<p><br /></p>

<hr />

<h1 id="-common-methods-to-increase-tps-and-their-limitations">🚀 Common Methods to Increase TPS and Their Limitations</h1>

<h2 id="commonly-attempted-approaches">Commonly Attempted Approaches</h2>

<ol>
  <li>Adding more servers (Scale Out)</li>
  <li>Increasing thread pool + DB connection pool sizes</li>
</ol>

<h2 id="limitations-of-these-methods">Limitations of These Methods</h2>

<p>→ When DB CPU utilization exceeds 70~80%:</p>
<ul>
  <li>DB query processing time increases</li>
  <li>Overall processing time lengthens</li>
  <li><strong>TPS actually decreases (counterproductive effect)</strong></li>
</ul>

<p><strong>⇒ Fundamentally, we need to reduce ‘processing time’ itself</strong></p>

<p><br /></p>

<hr />

<h1 id="-methods-to-reduce-processing-time">⚡ Methods to Reduce Processing Time</h1>

<h2 id="1-database-optimization">1. Database Optimization</h2>

<h3 id="1-1-query-tuning">1-1. Query Tuning</h3>

<h4 id="index-optimization">Index Optimization</h4>
<ul>
  <li>Apply appropriate indexes to frequently queried columns
    <ul>
      <li>B-tree: For range searches and sorting</li>
      <li>Hash: For equality comparisons (=) only</li>
      <li>Composite Index: When querying multiple columns together</li>
    </ul>
  </li>
</ul>

<h4 id="optimize-where-join-order-by-group-by-clauses">Optimize WHERE, JOIN, ORDER BY, GROUP BY Clauses</h4>
<ul>
  <li>Consider indexes for columns used in these clauses</li>
  <li><strong>Caution</strong>: DB optimizer may ignore indexes and choose Full Scan when query ratio is high
    <ul>
      <li>Threshold varies by DB (typically 5~15% of total data)</li>
    </ul>
  </li>
</ul>

<h4 id="avoid-unnecessary-select-">Avoid Unnecessary SELECT *</h4>
<ul>
  <li>Explicitly select only needed columns</li>
  <li>Reduces network transmission and improves DB memory efficiency</li>
</ul>

<h4 id="join-optimization">JOIN Optimization</h4>

<p><strong>1. JOIN Order Optimization</strong></p>
<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- ❌ Wrong: Query large table first</span>
<span class="k">SELECT</span> <span class="o">*</span> 
  <span class="k">FROM</span> <span class="n">orders</span> <span class="n">o</span>  <span class="c1">-- 10 million records</span>
  <span class="k">JOIN</span> <span class="n">countries</span> <span class="k">c</span> <span class="k">ON</span> <span class="n">o</span><span class="p">.</span><span class="n">country_id</span> <span class="o">=</span> <span class="k">c</span><span class="p">.</span><span class="n">id</span>  <span class="c1">-- 200 records</span>

<span class="c1">-- ✅ Correct: Query small table first</span>
<span class="k">SELECT</span> <span class="o">*</span> 
  <span class="k">FROM</span> <span class="n">countries</span> <span class="k">c</span>  <span class="c1">-- 200 records</span>
  <span class="k">JOIN</span> <span class="n">orders</span> <span class="n">o</span> <span class="k">ON</span> <span class="k">c</span><span class="p">.</span><span class="n">id</span> <span class="o">=</span> <span class="n">o</span><span class="p">.</span><span class="n">country_id</span>  <span class="c1">-- 10 million records</span>
</code></pre></div></div>

<p><strong>Caveats</strong>:</p>
<ul>
  <li>Modern DBs like PostgreSQL automatically optimize JOIN order, but not perfectly</li>
  <li>Optimization may fail with outdated statistics, functions, subqueries, or CTEs</li>
</ul>

<p><strong>2. Existence Checks</strong></p>
<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- ✅ Use EXISTS: Terminates immediately upon first match</span>
<span class="k">SELECT</span> <span class="o">*</span> 
  <span class="k">FROM</span> <span class="n">users</span> 
 <span class="k">WHERE</span> <span class="k">EXISTS</span> <span class="p">(</span>
  <span class="k">SELECT</span> <span class="mi">1</span> 
    <span class="k">FROM</span> <span class="n">orders</span> 
   <span class="k">WHERE</span> <span class="n">orders</span><span class="p">.</span><span class="n">user_id</span> <span class="o">=</span> <span class="n">users</span><span class="p">.</span><span class="n">id</span>
<span class="p">);</span>

<span class="c1">-- ❌ Using IN: Loads entire subquery result into memory</span>
<span class="k">SELECT</span> <span class="o">*</span> 
  <span class="k">FROM</span> <span class="n">users</span> 
 <span class="k">WHERE</span> <span class="n">id</span> <span class="k">IN</span> <span class="p">(</span>
  <span class="k">SELECT</span> <span class="n">user_id</span> 
    <span class="k">FROM</span> <span class="n">orders</span>
<span class="p">);</span>
</code></pre></div></div>

<p><strong>⇒ Conclusion: Minimize JOINs, use EXISTS for existence checks, avoid IN when possible</strong></p>

<h4 id="analyze-execution-plans-with-explainprofile">Analyze Execution Plans with EXPLAIN/PROFILE</h4>
<ul>
  <li>Check how queries are executed</li>
  <li>Verify Full Table Scan occurrences</li>
  <li>Confirm index usage</li>
</ul>

<h4 id="batch-processing">Batch Processing</h4>
<ol>
  <li><strong>Always</strong> bulk process multiple INSERT, UPDATE, DELETE operations
```sql
– ❌ Individual processing
INSERT INTO users VALUES (1, ‘A’);
INSERT INTO users VALUES (2, ‘B’);</li>
</ol>

<p>– ✅ Bulk processing
INSERT INTO users VALUES (1, ‘A’), (2, ‘B’), (3, ‘C’);</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
2. Reduce I/O count by grouping transactions

**⇒ Core of DB optimization: Minimize I/O + Reduce CPU burden**

&lt;br&gt;

### 📌 I/O Bound vs CPU Bound Operations

#### What are I/O Bound Operations?
1. Barely use CPU
2. Most time spent waiting for external resources
3. Examples:
    - Network communication
    - Disk read/write
    - DB queries
    - File I/O
    - Waiting for external API responses

**Problem**: Threads are blocked doing nothing while waiting for I/O → Thread resource waste

&lt;br&gt;

### 1-2. Caching

Store data in memory to reduce DB query execution
- Utilize in-memory caches like Redis, Memcached
- Application-level caches (Caffeine, Guava Cache, etc.)

### 1-3. Database Infrastructure Improvements

**Read/Write Splitting**
- Master: Write operations
- Slave (Replica): Read operations
- Distributes traffic to reduce load

**Scale Up**
- Improve hardware specs: CPU, memory, SSD
- Consider cost-effectiveness

### 1-4. Connection Pool Optimization

Optimize connection pool settings for DB, HTTP Client, Redis, etc.
- `maximumPoolSize`: Maximum number of connections
- `minimumIdle`: Minimum idle connections
- `connectionTimeout`: Connection wait time

**Benefits**:
- Prevent excessive connection creation/disposal
- Save CPU and memory
- Maintain stable performance

&lt;br&gt;

## 2. External API Call Optimization

### 2-1. Caching

Cache external API responses that don't change frequently

### 2-2. Remove Synchronous Calls

Asynchronous processing through message queues instead of direct calls
- Utilize Kafka, RabbitMQ, AWS SQS, etc.
- External system failures don't propagate

### 2-3. Asynchronous Processing

**Java Example**:
```java
CompletableFuture.supplyAsync(() -&gt; {
    return externalApiService.call();
});
</code></pre></div></div>

<p><strong>Important</strong>:</p>
<ul>
  <li><strong>Async processing is not for performance improvement, but for request thread protection and isolation</strong></li>
  <li>Total processing time may actually increase</li>
  <li>Main purpose is improving user experience and system stability</li>
</ul>

<p><br /></p>

<h2 id="3-data-aggregation-and-calculation-optimization">3. Data Aggregation and Calculation Optimization</h2>

<h3 id="problems-with-real-time-calculation">Problems with Real-time Calculation</h3>

<p>Calculating likes, views, followers, etc. each time:</p>
<ul>
  <li>Executes complex JOIN or COUNT queries</li>
  <li>Increases DB load</li>
  <li><strong>Drastically decreases TPS</strong></li>
</ul>

<h3 id="solutions">Solutions</h3>

<h4 id="pre-aggregation">Pre-Aggregation</h4>
<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- ❌ Calculate every time</span>
<span class="k">SELECT</span> <span class="k">COUNT</span><span class="p">(</span><span class="o">*</span><span class="p">)</span> 
  <span class="k">FROM</span> <span class="n">likes</span> 
 <span class="k">WHERE</span> <span class="n">post_id</span> <span class="o">=</span> <span class="mi">123</span><span class="p">;</span>

<span class="c1">-- ✅ Pre-calculate and store</span>
<span class="k">UPDATE</span> <span class="n">posts</span> 
   <span class="k">SET</span> <span class="n">like_count</span> <span class="o">=</span> <span class="n">like_count</span> <span class="o">+</span> <span class="mi">1</span> 
 <span class="k">WHERE</span> <span class="n">id</span> <span class="o">=</span> <span class="mi">123</span><span class="p">;</span>
</code></pre></div></div>

<h4 id="periodic-aggregation-with-batch-jobs">Periodic Aggregation with Batch Jobs</h4>
<ul>
  <li>Process data that doesn’t require real-time accuracy in batches</li>
  <li>Example: Recalculate statistics every hour</li>
</ul>

<h4 id="eventually-consistent-strategy">Eventually Consistent Strategy</h4>
<ul>
  <li>Allow slight data inconsistency</li>
  <li>Guarantee consistency eventually</li>
  <li>Example: When 1-2 second delay in like count is acceptable</li>
</ul>

<p><br /></p>

<hr />

<h1 id="️-methods-to-reduce-waiting-time">⏱️ Methods to Reduce Waiting Time</h1>

<p><strong>Response Time = Waiting Time + Processing Time</strong></p>

<p>Waiting time is primarily related to network transmission</p>

<h2 id="bandwidth">Bandwidth</h2>

<p>Maximum amount of data that can be transmitted</p>
<ul>
  <li>Small bandwidth causes drastically slower transmission speed when concurrent users increase</li>
</ul>

<h3 id="reduce-response-size">Reduce Response Size</h3>

<p><strong>Compression</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Content-Encoding: gzip
</code></pre></div></div>
<ul>
  <li>Compress HTTP response body</li>
  <li>Typically reduces size by 70~90%</li>
</ul>

<p><strong>Image Optimization</strong></p>
<ul>
  <li>Use WebP format</li>
  <li>Set appropriate resolution and quality</li>
  <li>Apply Lazy Loading</li>
</ul>

<p><strong>JSON Response Optimization</strong></p>
<ul>
  <li>Remove unnecessary fields</li>
  <li>Simplify data structure</li>
</ul>

<h3 id="separate-traffic">Separate Traffic</h3>

<p><strong>Utilize CDN (Content Delivery Network)</strong></p>
<ul>
  <li>Serve static files (images, CSS, JS) from CDN</li>
  <li>Reduce origin server load</li>
  <li>Serve from edge servers close to users → Reduce latency</li>
</ul>

<p><strong>Example</strong>:</p>
<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">&lt;!-- ❌ Serve directly from origin server --&gt;</span>
<span class="nt">&lt;img</span> <span class="na">src=</span><span class="s">"/static/images/logo.png"</span><span class="nt">&gt;</span>

<span class="c">&lt;!-- ✅ Serve through CDN --&gt;</span>
<span class="nt">&lt;img</span> <span class="na">src=</span><span class="s">"https://cdn.example.com/images/logo.png"</span><span class="nt">&gt;</span>
</code></pre></div></div>

<h3 id="increase-bandwidth">Increase Bandwidth</h3>

<p>Upgrade instance specifications</p>
<ul>
  <li>Network bandwidth varies by instance type in AWS EC2</li>
  <li>Example: t3.medium (up to 5 Gbps) → c5n.large (up to 25 Gbps)</li>
  <li><strong>Downside</strong>: Increased cost</li>
</ul>

<p><br /></p>

<hr />

<h1 id="-conclusion">✅ Conclusion</h1>

<p>Performance optimization requires a multi-layered approach, not simply adding more servers:</p>

<h2 id="core-strategies">Core Strategies</h2>

<ol>
  <li><strong>DB Optimization</strong>: Query tuning, index optimization, I/O minimization</li>
  <li><strong>Caching</strong>: Remove unnecessary operations and DB queries</li>
  <li><strong>Asynchronous Processing</strong>: Secure thread efficiency and system isolation</li>
  <li><strong>Pre-Aggregation</strong>: Use pre-calculated results instead of real-time calculation</li>
  <li><strong>Network Optimization</strong>: Compression, CDN, bandwidth management</li>
</ol>

<h2 id="cautions">Cautions</h2>

<ul>
  <li><strong>Root cause resolution takes priority</strong> over unconditional scale-out</li>
  <li>Async is not a silver bullet - <strong>use according to purpose</strong></li>
  <li>Caching must consider <strong>data consistency issues</strong></li>
  <li>Optimization should be <strong>measurement-based</strong> (no guessing)</li>
</ul>

<p>By systematically applying these methods, you can build a stable, scalable, high-performance system!</p>]]></content><author><name>Lee Sohyun</name></author><category term="Development" /><summary type="html"><![CDATA[]]></summary></entry><entry xml:lang="ko"><title type="html">SRT/KTX 예매 시스템 구현 및 JMeter 부하 테스트</title><link href="http://localhost:4000/ko/blog/train-reservations-jmeter-test/" rel="alternate" type="text/html" title="SRT/KTX 예매 시스템 구현 및 JMeter 부하 테스트" /><published>2026-02-01T00:00:00+09:00</published><updated>2026-02-01T00:00:00+09:00</updated><id>http://localhost:4000/ko/blog/train-reservations-jmeter-test</id><content type="html" xml:base="http://localhost:4000/ko/blog/train-reservations-jmeter-test/"><![CDATA[<h2 id="-프로젝트-개요">🎯 프로젝트 개요</h2>

<p><a href="/ko/blog/how-to-reserve-srt-or-ktx/">SRT/KTX 예매 시스템 설계</a>에서 설명한 설계를 실제로 구현하고 JMeter를 사용한 부하 테스트를 진행했습니다.</p>

<hr />

<h2 id="-github-레포지토리">📦 GitHub 레포지토리</h2>

<p><a href="https://github.com/leesohyeon96/train-reservations-jmeter-test" target="_blank">🔗 train-reservations-jmeter-test</a></p>

<hr />

<h2 id="-구현-단계별-개선-사항">🚀 구현 단계별 개선 사항</h2>

<h3 id="step1-기본-구현">Step1: 기본 구현</h3>
<ul>
  <li><strong>기술 스택</strong>: JPA + 비관적 락</li>
  <li><strong>목적</strong>: 기본적인 동시성 제어 테스트</li>
  <li><strong>문제점</strong>: 높은 트래픽에서 성능 저하 및 데드락 발생</li>
</ul>

<h3 id="step2-redis-queue-도입">Step2: Redis Queue 도입</h3>
<ul>
  <li><strong>기술 스택</strong>: JPA + Redis Queue + Lua 스크립트</li>
  <li><strong>개선 사항</strong>:
    <ul>
      <li>동시성 충돌 문제 해결</li>
      <li>예약 순서 보장</li>
    </ul>
  </li>
  <li><strong>핵심</strong>: Lua 스크립트로 원자적 처리 보장</li>
</ul>

<h3 id="step3-성능-최적화">Step3: 성능 최적화</h3>
<ul>
  <li><strong>기술 스택</strong>: JPA + Redis Cluster + 배치 처리 강화</li>
  <li><strong>개선 사항</strong>:
    <ul>
      <li>Redis Connection Pool 최적화 (최대 20개 연결)</li>
      <li>배치 크기: 100 → <strong>500</strong>으로 증가</li>
      <li>처리 주기: 500ms → <strong>200ms</strong>로 단축</li>
      <li>Worker 스레드: 4개 → <strong>8개</strong>로 증가</li>
      <li>좌석 정보 캐싱 (TTL: 30분)</li>
      <li>예약 내역 캐싱 (TTL: 5분)</li>
    </ul>
  </li>
</ul>

<h3 id="step4-1-rabbitmq-도입">Step4-1: RabbitMQ 도입</h3>
<ul>
  <li><strong>기술 스택</strong>: JPA + Redis (재고 관리) + RabbitMQ</li>
  <li><strong>특징</strong>:
    <ul>
      <li>Direct Exchange 사용</li>
      <li>Dead Letter Queue (DLQ) 활용</li>
      <li>메시지 지속성 보장</li>
      <li>Management UI 제공</li>
    </ul>
  </li>
</ul>

<h3 id="step4-2-kafka-도입">Step4-2: Kafka 도입</h3>
<ul>
  <li><strong>기술 스택</strong>: JPA + Redis (재고 관리) + Kafka</li>
  <li><strong>특징</strong>:
    <ul>
      <li>3개 파티션으로 병렬 처리</li>
      <li>Consumer Group 기반 확장</li>
      <li>파티션 내 순서 보장</li>
      <li>높은 처리량</li>
    </ul>
  </li>
</ul>

<hr />

<h2 id="-step4-1-vs-step4-2-비교">📊 Step4-1 vs Step4-2 비교</h2>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>RabbitMQ</th>
      <th>Kafka</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>구조</td>
      <td>Exchange → Queue</td>
      <td>Topic → Partition</td>
    </tr>
    <tr>
      <td>순서 보장</td>
      <td>Queue 내 순서 보장</td>
      <td>파티션 내 순서 보장</td>
    </tr>
    <tr>
      <td>처리량</td>
      <td>중간</td>
      <td>매우 높음</td>
    </tr>
    <tr>
      <td>확장성</td>
      <td>Consumer 수 증가</td>
      <td>파티션 수 증가</td>
    </tr>
    <tr>
      <td>특징</td>
      <td>Dead Letter Queue</td>
      <td>오프셋 관리</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="-테스트-방법">🧪 테스트 방법</h2>

<ol>
  <li>Docker Compose로 인프라 실행 (Redis/RabbitMQ/Kafka)</li>
  <li>애플리케이션 실행</li>
  <li>JMeter 테스트 플랜 실행</li>
  <li>성능 메트릭 확인</li>
</ol>

<hr />

<h2 id="-관련-글">📝 관련 글</h2>

<ul>
  <li><a href="/ko/blog/how-to-reserve-srt-or-ktx/">SRT/KTX 예매 시스템 설계</a> - 시스템 설계 및 아키텍처 설명</li>
</ul>]]></content><author><name>이소현</name></author><category term="자유" /><summary type="html"><![CDATA[🎯 프로젝트 개요]]></summary></entry><entry xml:lang="en"><title type="html">SRT/KTX Reservation System Design</title><link href="http://localhost:4000/en/blog/how-to-reserve-srt-or-ktx/" rel="alternate" type="text/html" title="SRT/KTX Reservation System Design" /><published>2026-02-01T00:00:00+09:00</published><updated>2026-02-01T00:00:00+09:00</updated><id>http://localhost:4000/en/blog/how-to-reserve-srt-or-ktx-en</id><content type="html" xml:base="http://localhost:4000/en/blog/how-to-reserve-srt-or-ktx/"><![CDATA[<p><br /></p>

<h1 id="-im-curious">🤔 I’m Curious</h1>
<p>As someone from the provinces who moved to Seoul,<br />
I started wondering how the <strong>holiday reservation war</strong> that repeats every Lunar New Year and Chuseok<br />
actually works internally,<br />
so I organized this design.</p>

<p><br /></p>

<hr />

<h1 id="ui-flow-explanation">UI Flow Explanation</h1>
<ol>
  <li>First, click [Holiday Train Reservation] right at 7 AM</li>
  <li>Then you enter the queue and can see how many people are waiting ahead of you</li>
  <li>When your queue number decreases and you get access, you enter a screen where you can search for trains and select seats</li>
  <li>Then you can reserve a seat</li>
  <li>At this time, payment is not made immediately but is available on a set date later!</li>
</ol>

<h2 id="so-let-me-imagine-and-explain-how-its-designed">So let me imagine and explain how it’s designed!!</h2>

<p><br /></p>

<hr />

<h2 id="1️⃣-reservation-start--entering-the-queue">1️⃣ Reservation Start – Entering the Queue</h2>
<p>At 7 AM, the moment a user clicks the <code class="language-plaintext highlighter-rouge">[Holiday Reservation]</code> button</p>

<ul>
  <li>Client → <strong>Redis ZSET-based queue registration</strong>
    <pre><code class="language-redis">ZADD waiting:queue 173820600012 user1
</code></pre>
  </li>
  <li>key: waiting:queue</li>
  <li>score: timestamp (entry time)</li>
  <li>value: userId</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">void</span> <span class="nf">enterQueue</span><span class="o">(</span><span class="nc">String</span> <span class="n">userId</span><span class="o">)</span> <span class="o">{</span>
    <span class="kt">long</span> <span class="n">now</span> <span class="o">=</span> <span class="nc">System</span><span class="o">.</span><span class="na">currentTimeMillis</span><span class="o">();</span>
    <span class="n">jedis</span><span class="o">.</span><span class="na">zadd</span><span class="o">(</span><span class="s">"waiting:queue"</span><span class="o">,</span> <span class="n">now</span><span class="o">,</span> <span class="n">userId</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>📌 Kafka Event Recording
At this time, simultaneously with Redis processing, produce an event to Kafka</p>

<h2 id="purpose-of-use">Purpose of Use</h2>
<ul>
  <li>Logging / Auditing
    <ul>
      <li>Who entered the queue and when?</li>
      <li>Who was issued a token?</li>
    </ul>
  </li>
  <li>Asynchronous Processing
    <ul>
      <li>Final DB storage</li>
      <li>Email / Push notifications</li>
      <li>External system integration</li>
    </ul>
  </li>
  <li>Failure Recovery
    <ul>
      <li>Queue reconstruction based on Kafka logs when Redis fails
Example events: USER_ENTER_QUEUE, TOKEN_ISSUED, USER_ADMITTED</li>
    </ul>
  </li>
</ul>

<p><br /></p>

<hr />

<h2 id="2️⃣-real-time-queue-position-display">2️⃣ Real-time Queue Position Display</h2>
<ul>
  <li>Check current position by querying Redis ZSET
    <pre><code class="language-redis">ZRANK waiting:queue user1
</code></pre>
  </li>
  <li>Read-only → Fast and safe</li>
  <li>Time complexity: O(log N)</li>
  <li>Query interval: 1~3 seconds</li>
  <li>Alternative: WebSocket / SSE + Redis Pub/Sub</li>
</ul>

<p><br /></p>

<hr />

<h2 id="3️⃣-admission-approval--token-issuance">3️⃣ Admission Approval &amp; Token Issuance</h2>
<p>If the reservation server allows 100 people <br />
Only the top 100 from the Redis queue are allowed entry</p>

<pre><code class="language-redis">ZRANGE waiting:queue 0 99
</code></pre>

<p>⚠️ Atomic processing with Lua Script: Process queue removal + token issuance as a single transaction</p>

<div class="language-lua highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> 
  <span class="n">redis</span><span class="p">.</span><span class="n">call</span><span class="p">(</span><span class="s2">"ZRANK"</span><span class="p">,</span> <span class="n">queueKey</span><span class="p">,</span> <span class="n">userId</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">limit</span> 
<span class="k">then</span>
  <span class="n">redis</span><span class="p">.</span><span class="n">call</span><span class="p">(</span><span class="s2">"ZREM"</span><span class="p">,</span> <span class="n">queueKey</span><span class="p">,</span> <span class="n">userId</span><span class="p">)</span>
  <span class="n">redis</span><span class="p">.</span><span class="n">call</span><span class="p">(</span><span class="s2">"SET"</span><span class="p">,</span> <span class="n">tokenKey</span><span class="p">,</span> <span class="n">userId</span><span class="p">,</span> <span class="s2">"EX"</span><span class="p">,</span> <span class="mi">300</span><span class="p">)</span>
<span class="k">end</span>
</code></pre></div></div>
<ul>
  <li>No mid-process failure ❌</li>
  <li>No duplicate issuance ❌</li>
  <li>No race conditions ❌</li>
</ul>

<h3 id="at-this-time-why-store-the-token-in-redis-after-issuance">At this time, why store the token in Redis after issuance?</h3>
<ul>
  <li>Storing tokens in DB causes DB bottleneck with thousands to tens of thousands of requests per second</li>
  <li>Client response example
    <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
</span><span class="nl">"accessToken"</span><span class="p">:</span><span class="w"> </span><span class="s2">"abc.def.ghi"</span><span class="p">,</span><span class="w">
</span><span class="nl">"expiresIn"</span><span class="p">:</span><span class="w"> </span><span class="mi">300</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div>    </div>
  </li>
</ul>

<p><br /></p>

<hr />

<h2 id="4️⃣-search-api--token-verification">4️⃣ Search API – Token Verification</h2>
<pre><code class="language-pgsql">GET /trains/search?from=Seoul&amp;to=hometown
Authorization: Bearer {queue-access-token}
</code></pre>
<ul>
  <li>Redis query:
    <pre><code class="language-redis">GET access:token:abc123
</code></pre>
  </li>
  <li>If exists → OK</li>
  <li>If not → 401 / 403</li>
</ul>

<p><br /></p>

<hr />

<h2 id="5️⃣-seat-search--seat-hold">5️⃣ Seat Search &amp; Seat Hold</h2>

<h3 id="seat-search-cache">Seat Search (Cache)</h3>
<pre><code class="language-redis">seat:availability:{trainId}:{date}
</code></pre>
<ul>
  <li>Seat status: Available / Hold / Sold</li>
</ul>

<h3 id="seat-hold-lock">Seat Hold (Lock)</h3>
<pre><code class="language-redis">SET seat:hold:{trainId}:{seatNo} userId NX EX 180
</code></pre>
<ul>
  <li>NX: Fails if seat is already held</li>
  <li>EX 180: Temporary hold for 3 minutes</li>
  <li>Automatically released when TTL expires</li>
</ul>

<h3 id="why-redis">Why Redis?</h3>
<ul>
  <li>Using only DB causes deadlock risk due to row lock contention</li>
</ul>

<p><br /></p>

<hr />

<h2 id="6️⃣-payment-stage">6️⃣ Payment Stage</h2>

<ul>
  <li>Proceed with payment after seat hold</li>
  <li>Payment involves external PG integration → The slowest section with highest failure probability</li>
  <li>Process after seat hold to distribute traffic</li>
</ul>

<p><br /></p>

<hr />

<p>✅ Conclusion</p>
<ul>
  <li>At reservation start time, manage user order with Redis ZSET-based queue and <br /> record events through Kafka to secure asynchronous processing and failure recovery capability</li>
  <li>At admission approval stage, use Lua Script to atomically process queue removal and token issuance</li>
  <li>Issued Queue Access Token is stored in Redis with TTL-based expiration</li>
  <li>Reservation APIs verify access rights through Redis query for every request</li>
  <li>Seat search uses Redis cache to reduce DB load and <br /> solve concurrency issues with NX + TTL-based Redis lock when holding seats</li>
  <li>Payment proceeds after seat hold → Traffic distribution</li>
  <li>Configure a reservation system that operates stably even in super-high-traffic environments like holidays by separating Redis roles into queue, cache, and lock</li>
</ul>

<hr />

<h2 id="-implementation--testing">🧪 Implementation &amp; Testing</h2>

<p>I actually implemented the design described above and conducted load testing using JMeter.</p>

<p>👉 Check out the implementation code and test results in <a href="/en/blog/train-reservations-jmeter-test/">SRT/KTX Booking System Implementation &amp; JMeter Load Test</a>.</p>]]></content><author><name>Lee Sohyun</name></author><category term="Development" /><summary type="html"><![CDATA[]]></summary></entry><entry xml:lang="ko"><title type="html">SRT/KTX 예매 시스템 설계</title><link href="http://localhost:4000/ko/blog/how-to-reserve-srt-or-ktx/" rel="alternate" type="text/html" title="SRT/KTX 예매 시스템 설계" /><published>2026-02-01T00:00:00+09:00</published><updated>2026-02-01T00:00:00+09:00</updated><id>http://localhost:4000/ko/blog/how-to-reserve-srt-or-ktx</id><content type="html" xml:base="http://localhost:4000/ko/blog/how-to-reserve-srt-or-ktx/"><![CDATA[<p><br /></p>

<h1 id="-궁금하다-궁금해">🤔 궁금하다 궁금해</h1>
<p>서울로 상경한 지방인으로서,<br />
설날·추석마다 반복되는 <strong>명절 예매 전쟁</strong>은 대체 내부적으로 어떻게 동작할까?<br />
라는 궁금증에서 시작해 설계를 정리해봤다.</p>

<p><br /></p>

<hr />

<h1 id="ui-동작방식-설명">UI 동작방식 설명</h1>
<ol>
  <li>일단 오전7시에 딱 [열차 명절예매]를 클릭함</li>
  <li>그럼 대기열에 들어가고 내 앞에 대기중인 사람들이 조회된다</li>
  <li>대기 순번이 줄어들어서 접속하게 되면 열차 검색 및 좌석 선택이 가능한 화면에 입장하게 된다</li>
  <li>그럼 좌석을 예매하면 된다</li>
  <li>이때, 결제는 바로 하지 않고 날짜를 정해 그때 가능함!</li>
</ol>

<h2 id="그럼-인제-어떻게-설계되있는지-상상을-해서-말해보자면">그럼 인제 어떻게 설계되있는지 상상을 해서 말해보자면!!</h2>

<p><br /></p>

<hr />

<h2 id="1️⃣-예매-시작--대기열-진입">1️⃣ 예매 시작 – 대기열 진입</h2>
<p>오전 7시, 사용자가 <code class="language-plaintext highlighter-rouge">[명절 예매]</code> 버튼을 누르는 순간</p>

<ul>
  <li>Client → <strong>Redis ZSET 기반 대기열 등록</strong>
    <pre><code class="language-redis">ZADD waiting:queue 173820600012 user1
</code></pre>
  </li>
  <li>key: waiting:queue</li>
  <li>score: timestamp (입장 시각)</li>
  <li>value: userId</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">void</span> <span class="nf">enterQueue</span><span class="o">(</span><span class="nc">String</span> <span class="n">userId</span><span class="o">)</span> <span class="o">{</span>
    <span class="kt">long</span> <span class="n">now</span> <span class="o">=</span> <span class="nc">System</span><span class="o">.</span><span class="na">currentTimeMillis</span><span class="o">();</span>
    <span class="n">jedis</span><span class="o">.</span><span class="na">zadd</span><span class="o">(</span><span class="s">"waiting:queue"</span><span class="o">,</span> <span class="n">now</span><span class="o">,</span> <span class="n">userId</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>📌 Kafka 이벤트 기록
이때, Redis 처리와 동시에 Kafka로 이벤트를 Produce 한다</p>

<h2 id="사용-목적">사용 목적</h2>
<ul>
  <li>로그 / 감사
    <ul>
      <li>누가 언제 대기열에 들어왔는가?</li>
      <li>누가 토큰을 발급받았는가?</li>
    </ul>
  </li>
  <li>비동기 처리
    <ul>
      <li>DB 최종 저장</li>
      <li>이메일 / 푸시 알림</li>
      <li>외부 시스템 연동</li>
    </ul>
  </li>
  <li>장애 복구
    <ul>
      <li>Redis 장애 시 Kafka 로그 기반 대기열 재구성
예시 이벤트: USER_ENTER_QUEUE, TOKEN_ISSUED, USER_ADMITTED</li>
    </ul>
  </li>
</ul>

<p><br /></p>

<hr />

<h2 id="2️⃣-실시간-대기-순번-표시">2️⃣ 실시간 대기 순번 표시</h2>
<ul>
  <li>Redis ZSET 조회로 현재 순번 확인
    <pre><code class="language-redis">ZRANK waiting:queue user1
</code></pre>
  </li>
  <li>읽기 전용 → 빠르고 안전</li>
  <li>시간 복잡도: O(log N)</li>
  <li>조회 주기: 1~3초</li>
  <li>대안: WebSocket / SSE + Redis Pub/Sub</li>
</ul>

<p><br /></p>

<hr />

<h2 id="3️⃣-입장-허용--토큰-발급">3️⃣ 입장 허용 &amp; 토큰 발급</h2>
<p>예약 서버 허용 인원이 100명이라면 <br />
Redis 대기열 상위 100명만 입장 허용</p>

<pre><code class="language-redis">ZRANGE waiting:queue 0 99
</code></pre>

<p>⚠️ Lua Script로 원자 처리: 대기열 제거 + 토큰 발급을 단일 트랜잭션으로 처리</p>

<div class="language-lua highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> 
  <span class="n">redis</span><span class="p">.</span><span class="n">call</span><span class="p">(</span><span class="s2">"ZRANK"</span><span class="p">,</span> <span class="n">queueKey</span><span class="p">,</span> <span class="n">userId</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">limit</span> 
<span class="k">then</span>
  <span class="n">redis</span><span class="p">.</span><span class="n">call</span><span class="p">(</span><span class="s2">"ZREM"</span><span class="p">,</span> <span class="n">queueKey</span><span class="p">,</span> <span class="n">userId</span><span class="p">)</span>
  <span class="n">redis</span><span class="p">.</span><span class="n">call</span><span class="p">(</span><span class="s2">"SET"</span><span class="p">,</span> <span class="n">tokenKey</span><span class="p">,</span> <span class="n">userId</span><span class="p">,</span> <span class="s2">"EX"</span><span class="p">,</span> <span class="mi">300</span><span class="p">)</span>
<span class="k">end</span>
</code></pre></div></div>
<ul>
  <li>중간 실패 ❌</li>
  <li>중복 발급 ❌</li>
  <li>경쟁 상태 ❌</li>
</ul>

<h3 id="이때-토큰-발급-후-왜-redis에-저장">이때, 토큰 발급 후 왜 Redis에 저장?</h3>
<ul>
  <li>DB에 토큰을 저장하면 초당 수천~수만 요청으로 DB 병목 발생</li>
  <li>Client 응답 예시
    <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
</span><span class="nl">"accessToken"</span><span class="p">:</span><span class="w"> </span><span class="s2">"abc.def.ghi"</span><span class="p">,</span><span class="w">
</span><span class="nl">"expiresIn"</span><span class="p">:</span><span class="w"> </span><span class="mi">300</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div>    </div>
  </li>
</ul>

<p><br /></p>

<hr />

<h2 id="4️⃣-검색-api--토큰-검증">4️⃣ 검색 API – 토큰 검증</h2>
<pre><code class="language-pgsql">GET /trains/search?from=Seoul&amp;to=hometown
Authorization: Bearer {queue-access-token}
</code></pre>
<ul>
  <li>Redis 조회:
    <pre><code class="language-redis">GET access:token:abc123
</code></pre>
  </li>
  <li>있으면 → OK</li>
  <li>없으면 → 401 / 403</li>
</ul>

<p><br /></p>

<hr />

<h2 id="5️⃣-좌석-조회--좌석-선점">5️⃣ 좌석 조회 &amp; 좌석 선점</h2>

<h3 id="좌석-조회-캐시">좌석 조회 (캐시)</h3>
<pre><code class="language-redis">seat:availability:{trainId}:{date}
</code></pre>
<ul>
  <li>좌석 상태: Available / Hold / Sold</li>
</ul>

<h3 id="좌석-선점-락">좌석 선점 (락)</h3>
<pre><code class="language-redis">SET seat:hold:{trainId}:{seatNo} userId NX EX 180
</code></pre>
<ul>
  <li>NX: 이미 선점된 좌석이면 실패</li>
  <li>EX 180: 3분간 임시 선점</li>
  <li>TTL 만료 시 자동 반환</li>
</ul>

<h3 id="왜-redis">왜 Redis?</h3>
<ul>
  <li>DB만 사용하면 row lock 경합으로 데드락 위험 발생</li>
</ul>

<p><br /></p>

<hr />

<h2 id="6️⃣-결제-단계">6️⃣ 결제 단계</h2>

<ul>
  <li>좌석 선점 이후 결제 진행</li>
  <li>결제는 외부 PG 연동 → 가장 느리고 실패 확률이 높은 구간</li>
  <li>좌석 선점 이후 처리하여 트래픽 분산</li>
</ul>

<p><br /></p>

<hr />

<p>✅ 결론</p>
<ul>
  <li>예매 시작 시점에는 Redis ZSET 기반 대기열로 사용자 순서를 관리하고 <br /> Kafka를 통해 이벤트를 기록해 비동기 처리와 장애 복구 가능성을 확보</li>
  <li>입장 허용 단계에서는 Lua Script를 사용해 대기열 제거와 토큰 발급을 원자적으로 처리</li>
  <li>발급된 Queue Access Token은 Redis에 TTL 기반으로 저장</li>
  <li>예약 API들은 매 요청마다 Redis 조회를 통해 접근 권한 검증</li>
  <li>좌석 조회는 Redis 캐시를 사용해 DB 부하를 줄이고 <br /> 좌석 선점 시 NX + TTL 기반 Redis 락으로 동시성 문제 해결</li>
  <li>결제는 좌석 선점 이후 진행 → 트래픽 분산</li>
  <li>Redis를 큐, 캐시, 락으로 역할 분리해 명절과 같은 초고트래픽 환경에서도 안정적으로 동작하는 예매 시스템 구성</li>
</ul>

<hr />

<h2 id="-실제-구현-및-테스트">🧪 실제 구현 및 테스트</h2>

<p>위에서 설명한 설계를 실제로 구현하고 JMeter를 사용한 부하 테스트를 진행했습니다.</p>

<p>👉 <a href="/ko/blog/train-reservations-jmeter-test/">SRT/KTX 예매 시스템 구현 및 JMeter 부하 테스트</a>에서 구현 코드와 테스트 결과를 확인할 수 있습니다.</p>]]></content><author><name>이소현</name></author><category term="개발" /><summary type="html"><![CDATA[]]></summary></entry><entry xml:lang="ko"><title type="html">Nexus에 대해 알아보자</title><link href="http://localhost:4000/ko/blog/nexus/" rel="alternate" type="text/html" title="Nexus에 대해 알아보자" /><published>2024-12-03T00:00:00+09:00</published><updated>2024-12-03T00:00:00+09:00</updated><id>http://localhost:4000/ko/blog/nexus</id><content type="html" xml:base="http://localhost:4000/ko/blog/nexus/"><![CDATA[<h1 id="nexus-사용-정리">Nexus 사용 정리</h1>

<p><br /></p>

<hr />

<p><br /></p>

<h2 id="-프로젝트-배경">🏢 프로젝트 배경</h2>

<ul>
  <li><strong>환경</strong>: <strong>폐쇄망</strong>(인터넷이 연결되지 않은 내부망)</li>
</ul>

<p><br /></p>

<hr />

<p><br /></p>

<h2 id="-폐쇄망-환경의-문제점">🔒 폐쇄망 환경의 문제점</h2>

<ul>
  <li>인터넷이 차단되어 있어 외부로부터 <strong>라이브러리(dependency)</strong> 를 자동으로 다운로드 받을 수 없음</li>
  <li>예: Gradle, Maven, npm, Docker 등에서 사용하는 <strong>의존성 패키지들</strong>을 가져올 수 없음</li>
</ul>

<p><br /></p>

<hr />

<p><br /></p>

<h2 id="-해결-방법--nexus-도입">📦 해결 방법 – Nexus 도입</h2>

<h3 id="-nexus란">✅ Nexus란?</h3>

<ul>
  <li>Sonatype에서 제공하는 <strong>Repository Manager</strong></li>
  <li>다양한 패키지 매니저(Maven, npm, Docker 등)의 <strong>프록시 역할</strong>을 해줌</li>
  <li>내부망에 Nexus를 설치하면 외부에서 미리 받은 라이브러리를 저장해두고<br />
폐쇄망 환경에서도 이를 <strong>로컬에서 가져다 쓸 수 있음</strong></li>
</ul>

<h3 id="-nexus의-주요-쓰임새">🎯 Nexus의 주요 쓰임새</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Dependency 저장소</strong></td>
      <td>외부망에서 미리 다운로드 받은 라이브러리들을(ex. jar파일) 보관</td>
    </tr>
    <tr>
      <td><strong>폐쇄망 대응</strong></td>
      <td>내부망에서 의존성 해결 가능</td>
    </tr>
    <tr>
      <td><strong>프록시 캐시</strong></td>
      <td>한번 받은 라이브러리는 캐시되어 재사용 가능</td>
    </tr>
    <tr>
      <td><strong>Docker Registry 기능</strong></td>
      <td><strong>자체 Docker 이미지 저장소</strong>로 활용 가능</td>
    </tr>
  </tbody>
</table>

<p>거의 Dependency 저장소로만 사용했고 사용할 라이브러리들을 remote로 배포시에 각각 올리기로 했음</p>

<p><br /></p>

<hr />

<p><br /></p>

<h2 id="-nexus와-docker">🐳 Nexus와 Docker</h2>

<ul>
  <li>Nexus는 <strong>Docker Registry 기능도 지원</strong>함</li>
  <li>따라서 사내에서 만든 Docker 이미지를 Nexus에 저장해두고<br />
폐쇄망 환경에서 pull/push 가능</li>
  <li>개발 및 배포 자동화에도 활용 가능</li>
  <li>ex) docker hub 같은 역할 &gt; 다만 private</li>
</ul>

<p><br /></p>

<hr />

<p><br /></p>

<blockquote>
  <p>📝 정리</p>
  <ul>
    <li>대상 프로젝트는 <strong>폐쇄망 환경</strong>이라 외부 의존성 사용이 불가능</li>
    <li>이를 해결하기 위해 <strong>Nexus</strong>를 사용해 의존성 사용할 수 있도록 함</li>
    <li>Nexus는 Maven/Docker 등 다양한 패키지 관리 시스템과 연동되며<br />
폐쇄망 개발 환경에서 <strong>중앙 저장소</strong> 역할을 수행</li>
  </ul>
</blockquote>

<p><br /></p>

<hr />

<p><br /></p>]]></content><author><name>이소현</name></author><category term="개념" /><summary type="html"><![CDATA[Nexus 사용 정리]]></summary></entry><entry xml:lang="ko"><title type="html">NAT vs Bridge</title><link href="http://localhost:4000/ko/blog/nat_vs_bridge/" rel="alternate" type="text/html" title="NAT vs Bridge" /><published>2024-11-13T00:00:00+09:00</published><updated>2024-11-13T00:00:00+09:00</updated><id>http://localhost:4000/ko/blog/nat_vs_bridge</id><content type="html" xml:base="http://localhost:4000/ko/blog/nat_vs_bridge/"><![CDATA[<h1 id="가상-서버-구성-및-nat-vs-bridge-정리">가상 서버 구성 및 NAT vs Bridge 정리</h1>

<p><br /></p>

<hr />

<p><br /></p>

<h2 id="-배경">🧩 배경</h2>
<p>회사에서 가상서버 구현하시는 거 구경하다가 알게된 사실 정리하는 글<br />
제공된 운영체제는 <strong>Windows OS</strong>였다</p>

<p><br /></p>

<hr />

<p><br /></p>

<h2 id="️-과정">⚙️ 과정</h2>
<p>Windows에서 가상화 소프트웨어(VMware)를 이용해 총 <strong>3개의 가상머신(VM)</strong> 을 생성함:</p>
<ul>
  <li>Application 서버</li>
  <li>DB 서버</li>
  <li>기타 서버 (무슨 목적이였는지 기억이 잘안남)</li>
</ul>

<p>VM을 만들 때 네트워크 설정을 해야 하는데, <strong>주로 두 가지 방식인 NAT와 Bridge</strong> 중에서 선택하게 됨<br />
이 두 방식은 가상머신이 외부 또는 내부 네트워크와 통신하는 방식에 큰 차이가 있음</p>

<p><br /></p>

<hr />

<p><br /></p>

<h2 id="-nat-vs-bridge--차이점-정리">🌐 NAT vs Bridge – 차이점 정리</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>NAT(Network Address Translation)</th>
      <th>Bridge(브리지 네트워크)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>IP 부여 방식</td>
      <td><strong>호스트 OS</strong> 가 DHCP 서버 역할을 하며 <strong>내부 IP</strong> 부여</td>
      <td>실제 물리 네트워크(DHCP 서버 or 공유기)에서 <strong>직접 IP 할당</strong></td>
    </tr>
    <tr>
      <td>외부 통신</td>
      <td>VM이 외부와 통신할 수 있지만, 외부에서 <strong>직접 접근 불가</strong></td>
      <td>외부에서도 VM에 <strong>직접 접근 가능</strong></td>
    </tr>
    <tr>
      <td>구조</td>
      <td>외부 → <strong>Host OS</strong> → VM (호스트를 경유)</td>
      <td>외부 → <strong>VM 직접 접근</strong></td>
    </tr>
    <tr>
      <td>보안</td>
      <td>상대적으로 <strong>안전함</strong> (내부망 느낌)</td>
      <td>외부에 노출될 수 있어 <strong>보안 설정 필요</strong></td>
    </tr>
    <tr>
      <td>용도</td>
      <td>개발/테스트 환경에 적합</td>
      <td>실제 서비스 환경, 여러 장비 간 통신 필요한 경우</td>
    </tr>
    <tr>
      <td>관련 개념</td>
      <td><strong>Jump Host</strong> 구조와 유사 <br /> (ex. <code class="language-plaintext highlighter-rouge">ProxyCommand</code>를 사용한 SSH 연결)</td>
      <td><strong>일반 서버처럼 독립 IP 사용</strong></td>
    </tr>
  </tbody>
</table>

<h5 id="proxycommand란">ProxyCommand란?</h5>
<ul>
  <li>SSH가 최종 목적지 서버에 접속하기 전에 <code class="language-plaintext highlighter-rouge">중간 서버를 통해 터널을 연결하는 명령어를 정의</code>하는 옵션임</li>
  <li>[내 컴퓨터] – SSH –&gt; [Jump Host] – SSH –&gt; [목적지 서버]
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh -o "ProxyCommand ssh user@jump.example.com nc private.server.local 22" user@private.server.local
</code></pre></div>    </div>
    <p>1) jump.example.com 서버에서 SSH로 접근함
2) 중간의 nc 명령어를 통해 private.sever.local:22 에 연결
3) 터널을 통해 최종 서버에 SSH 접속 완료</p>
  </li>
</ul>

<p><br /></p>

<hr />

<p><br /></p>

<h2 id="-추가-설명-비유">💡 추가 설명 (비유)</h2>

<p><strong>NAT</strong><br />
  → 마치 집에서 <strong>공유기(Wi-Fi)</strong> 를 사용하는 것과 같음
  VM은 <strong>사설 IP</strong>(예: 192.168.x.x)를 가지고 있고, 외부와 통신할 때는 <strong>호스트를 통해 나감</strong><br />
  외부에서 직접 이 VM으로 들어올 수 없음</p>

<p><strong>Bridge</strong><br />
  → VM도 마치 <strong>직접 랜선 꽂은 일반 PC</strong>처럼 동작
  공유기에서 직접 공인 IP 또는 사설 IP를 받아서, 같은 네트워크 내에 있는 다른 장치들과 <strong>동등하게 통신 가능</strong></p>

<p><br /></p>

<hr />

<p><br /></p>

<h2 id="-정리">✅ 정리</h2>

<blockquote>
  <p><strong>NAT</strong> 방식은 개발자 혼자 테스트할 때 적합하고,<br />
<strong>Bridge</strong> 방식은 실제 서버처럼 구성할 때 유용함
보안, 접근성, 네트워크 정책에 따라 적절한 방식을 선택하는 것이 중요함</p>
</blockquote>]]></content><author><name>이소현</name></author><category term="개념" /><summary type="html"><![CDATA[가상 서버 구성 및 NAT vs Bridge 정리]]></summary></entry></feed>